规范包名小写
No qualifying bean of type 'com.demomodel.configure.doubledatasource.textdoubledatasource.master.map.DemotxtMapper' available: expected at least 1 bean which qualifies as autowire candidate. Dependency annotations: {@org.springframework.beans.factory.annotation.Autowired(required=true)}
错误的原因是xml配置不对   二检查mapper  service  三  @MapperScan({"com.demomodel.mysqlcontroller.mapper","com.demomodel.configure.doubledatasource.textdoubledatasource.secondary.map"}) 说明map在哪里

通过@PropertySource指明properties文件的位置，然后通过@Value注入值
@PropertySource({  
    "classpath:config.properties",  
    "classpath:db.properties" //如果是相同的key，则最后一个起作用  
})  


@EnableAutoConfiguration(exclude={DataSourceAutoConfiguration.class})
自动注入，否则该配置文件中的配置无效。会报出Failed to configure a DataSource: ‘url’ 
attribute is not specified and no embedded datasource could be configured.错误
————————————————
版权声明：本文为CSDN博主「qq8693」的原创文章，遵循 CC 4.0 BY-SA 版权协议，转载请附上原文出处链接及本声明。
原文链接：https://blog.csdn.net/qq8693/java/article/details/86166953
//把spring boot自动初始化DataSource相关的代码禁止掉
@SpringBootApplication(exclude = { DataSourceAutoConfiguration.class, DataSourceTransactionManagerAutoConfiguration.class })


启动配置说明
@SpringBootApplication  //把spring boot自动初始化DataSource相关的代码禁止掉
//@SpringBootApplication(exclude = { DataSourceAutoConfiguration.class, DataSourceTransactionManagerAutoConfiguration.class })
//@SpringBootApplication(exclude = DataSourceAutoConfiguration.class)
问题  If you want an embedded database (H2, HSQL or Derby), please put it on the classpath.
	If you have database settings to be loaded from a particular profile you may need to activate it (no profiles are currently active)
也可以不配置   p配置类没配置数据源就用@SpringBootApplication(exclude = DataSourceAutoConfiguration.class)

//@PropertySource({ "classpath:quthor.properties"})
@MapperScan({"com.demomodel.mysqlcontroller.mapper"}) //扫描mapper注解  
//扫描包
//@ComponentScan(basePackages = {"com"})  //多个可以用，分割
@EnableAutoConfiguration(exclude={DataSourceAutoConfiguration.class})
@ServletComponentScan   //启动器启动时，扫描本目录以及子目录带有的webservlet注解的  
//@EnableScheduling //开启定时任务
//@ImportResource({"classpath:some-context.xml","classpath:another-context.xml"}) //支持整合xml